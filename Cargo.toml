[package]
name = "zoea"
version = "0.1.0"
edition = "2021"

[dependencies]
bootloader = "0.9.23"

[profile.dev]
panic = "abort" # disable stack unwinding on panic

[profile.release]
panic = "abort" # disable stack unwinding on panic